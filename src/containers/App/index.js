import React from 'react';
import { Grid, Typography, Divider } from "@material-ui/core";

import "./styles.scss";

const App = () => {
  return <Grid className="app">
    <Typography className="title" variant="h4">Exercice :</Typography>
    <Divider />
    {/* Mettre le code ici */}
  </Grid>
};

export default App;
